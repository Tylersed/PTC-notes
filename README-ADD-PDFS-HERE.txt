This folder is optional.

The current safe version opens Google Drive for printing so users do not see missing-file errors.

Only add PDFs here if you want true one-click local PDF printing later. If you add PDFs, their filenames must match PRINT-FILE-MAP.csv and the matching resource item must have a printUrl value.
